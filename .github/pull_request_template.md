## Summary
<!-- What does this PR do and why? For a lean-loop PR: which docs/lean/ spec it builds. -->

## Changes
-

## Test plan
- [ ] `python3 -m unittest discover -s tests -q` passes, and the test count matches the spec
- [ ] No test starts zg, Jev (OpenRouter) or an LLM for real, or opens a network connection
- [ ] No new runtime dependency (stdlib only, Python 3.10+)
- [ ] Manual run against a real indexed corpus, if behaviour visible to a user changed

## Related issues
<!-- Closes #NNN -->
